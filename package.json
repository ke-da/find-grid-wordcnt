{
  "name": "mongo-model",
  "version": "0.1.0",
  "dependencies": {
    "mongodb": "^1.4.7",
    "bluebird": "^2.2.2"
  },
  "scripts": {
    "test": "./node_modules/.bin/jasmine-node --color --autotest --watch run.js --test-dir ./spec/"
  },
  "engines": {
    "node": "~0.10",
    "npm": "~1.2"
  }
}
